  {{#if session.isAuthenticated}}
  <button class="btn btn-lg btn-primary btn-block" type="submit" {{action 'signOut'}}>SignOut</button>
  {{else}}
  <button class="btn btn-lg btn-primary btn-block" disabled="true" type="submit" {{action 'signOut'}}>SignOut</button>
  {{/if}}
